INTERFACE = "InterfaceAccess/form.ui"

DETECTION_MODEL = "MODELS/IRpeople45.pt"
DETECTION_TRACKER = "botsort.yaml" # either botsort.yaml or bytetrack.yaml

# TODO: Change this so that the servers are saved locally (probably in .env or .json)/
# Idea: Save everything in a settings.json file and add it to .gitignore
# This way, the user can change the server settings without changing the code

# The .env will be overwritten by the chosen Json file, so that the "Default Server" can be chosen.
SERVER_LOCATION = "localhost"
SERVER_PORT = 1433
SERVER_USER = "sa"
SERVER_PASSWORD = "N0t3431@lv"
DATABASE_NAME = "master"